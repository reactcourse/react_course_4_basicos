import React from 'react';

const Footer = (props)=>{

    return (
        <footer>
            <p>All Rights reserved &copy; {props.currentYear}</p>            
        </footer>

    )
}

export default Footer;